#include <iostream>
using namespace std;

int main() {

    int month, year;
    bool leapYear;

    do
    {
        cout << "Enter month and year (MM YY) or (0 0) to quit: ";
        cin >> month >> year;

    } while (month < 0 || month > 12 || );
    

    return 0;
}